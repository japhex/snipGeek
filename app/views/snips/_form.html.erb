<%= form_for @snip, :remote => true do |f| %>
  <%= f.text_field :name %>
  <% f.fields_for :snippets do |builder| %>
    <%= render 'newsnippet', :f => builder %>			
	<% end %>
	<%= f.submit%>
<% end %>